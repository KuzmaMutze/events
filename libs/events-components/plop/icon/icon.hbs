import { Icon, IconProps } from '../icon.styled';
import { forwardRef } from 'react';

export const {{iconName}}Icon = forwardRef<SVGSVGElement, IconProps>((props, ref) => {
    return (
        {{svg}}
    );
});