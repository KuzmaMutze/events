import { {{ capitalize componentName }} } from '@events-components/{{ dashCase componentName }}';

export default {
    title: '{{ capitalize componentName }}',
    component: {{ capitalize componentName }}
}

export const Basic = () => {
    return <{{ capitalize componentName }}/>
}
